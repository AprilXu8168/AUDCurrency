using NUnit.Framework;
using ExchangeRatesService.Services;

namespace ExchangeRatesService.Tests;
    [TestFixture]
    public class ExchangeRatesServiceTest
    {
    private ExChangeService _exchangeServer;

    [SetUp]
    public void SetUp()
    {
        _exchangeServer = new ExChangeService();
    }

    [Test]
    public void IsServer_Running()
    {
        var result = _exchangeServer.GetCurrencyPairs;

        Assert.That(result, Is.Not.Null, "Result should not be Null");
    }
}